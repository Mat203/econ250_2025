version: 2

models:
  - name: stg_assignment3_uk_wiki
    columns:
      - name: device_type
        tests:
          - not_null
          - accepted_values:
              values: ['desktop', 'mobile']